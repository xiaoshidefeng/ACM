#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<math.h>
int main()
{
	double a,x,y=1,e=2,j;
	int n;
	x=sqrt(5);
	j=1/sqrt(5);
	while(scanf("%lf",&n)!=EOF)
	{
		//a=double(log10(double(1)/x))+(double)(log10(pow((double(1)+x)/double(2),n)-(pow((double(1)-x)/double(2))));
		a=log10(a)+(log10(pow((y+x)/e),(double)(n))-pow((y-x)/e),(double)(n)));
	}
}